#define RX2 16
#define TX2 17

void setup() {
  Serial.begin(115200);
  Serial2.begin(115200, SERIAL_8N1, RX2, TX2);
}

void loop() {
  if (Serial2.available() > 0){
    Serial.println(Serial2.readString());
  }
}
